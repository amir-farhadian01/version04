# =============================================================================
# Neighborly — Startup Commands
# =============================================================================
# ✅ ONLY ONE COMMAND YOU NEED:
# =============================================================================

./scripts/start-all.sh --all

# =============================================================================
# That's it. One command. It does everything:
#   1. Starts Docker (PostgreSQL)
#   2. Starts Backend API (port 8080)
#   3. Starts Admin API (port 9090)
#   4. Starts React Frontend (port 5173)
#   5. Starts Flutter Web (port 7357)
#
# Press Ctrl+C to stop everything.
# =============================================================================
