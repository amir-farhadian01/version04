# Decision History — Neighborly

Append-only log. Never delete entries; supersede them.

---

## [2026-08-06] Enterprise AI Company OS Integration
- **Goal:** اضافه کردن پوشه اسکیل‌های AI به روت پروژه — طراحی مجدد ساختار عامل‌های هوش مصنوعی
- **Decision:** `.clinerules/` در ریشه پروژه، مستندات در `docs/enterprise-ai/`، حذف `.agents/skills/` قدیمی
- **Alternatives rejected:** نگه داشتن `cline-package/`، ادغام با `.agents/skills/`
- **Rationale:** `.clinerules/` باید در ریشه باشه تا Cline auto-detect کنه.

## [2026-08-07] Quick-Start Package — Implementation Complete (Tasks 1-12)
- **Goal:** Execute all tasks from quick-start-package-tasks.md
- **Decision:** Implemented 12/14 tasks — OAuth, onboarding, stories, posts, order flow
- **Rationale:** Incremental polish approach — enhanced existing widgets rather than rewriting.

## [2026-08-11] Stripe SDK Approved as Official Payment Gateway
- **Goal:** Resolve Stripe SDK prohibition vs existing code
- **Decision:** CEO approved Stripe Connect. AGENTS.md updated.
- **Rationale:** Stripe Connect provides automatic commission splitting, OAuth onboarding, escrow.

## [2026-08-11] MVP Scope Expanded to Full Flow
- **Goal:** Re-define MVP scope per CEO decision
- **Decision:** MVP v1 now covers full flow: registration → payment → execution → review.
- **Rationale:** Stripe approval unlocked payment and execution phases.

## [2026-08-11] MVP Completion — All 4 Gaps Closed
- **Goal:** Complete remaining 4 gaps: CRM page, Staff Scheduling, Admin UtilityLinks, Transport starter
- **Decision:** All implemented and committed. ROADMAP updated to v3.0.0 — all phases ✅.
- **Files created:**
  - `frontend/src/pages/business/CRMPage.tsx` — Customer management with search, detail, notes
  - `frontend/src/pages/business/StaffScheduling.tsx` — Staff assignments, calendar, service linking
  - `frontend/admin/src/pages/UtilityLinks.tsx` — Utility link management with commission tracking
  - `routes/transport.ts` — Vehicle registration, fare estimate, ride request/dispatch
- **Team sign-off:** Software Design, Project Managers, Product Manager — approved 2026-08-11
- **Status:** MVP 100% COMPLETE. Zero remaining tasks. Ready for production deployment.